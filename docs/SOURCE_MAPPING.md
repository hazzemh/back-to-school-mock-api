# Source Mapping

The initial mock fixtures were extracted directly from the supplied files:

| Mock fixture | Supplied source |
|---|---|
| `app/mock_data/service_assurance.json` | `b2s1-apis.txt` |
| `app/mock_data/complaint_intelligence.json` | `b2s2-apis(1).txt` |
| `app/mock_data/service_operations.json` | `b2s3-apis(1).txt` |

The supplied Interface Specification Document was used as the contract reference for the shared envelope, payload versions, endpoint inventory, and unresolved/TBD request/error/security decisions.
